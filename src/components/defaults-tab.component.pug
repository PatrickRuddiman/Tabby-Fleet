.fleet-defaults-tab
  h3 Agent Fleet defaults
  p.text-muted
    | These settings seed every new Agent Fleet profile. Existing profiles keep their current values.
  agent-fleet-profile-settings([profile]='profile' (change)='onChange()')
